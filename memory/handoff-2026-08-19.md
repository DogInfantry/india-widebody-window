# Handoff, 2026-08-19

**Read `CLAUDE.md` first.** It is current as of this handoff and is the authoritative file.
This snapshot records what `CLAUDE.md` cannot: what happened in this session, what was
decided, and what a new session should and should not do.

---

## One-line status

**India's Wide-Body Window is finished.** 195 tests pass, build green on 10 routes, working
tree clean, everything pushed, `main` at `f1704fe`. Live on Vercel and GitHub Pages.

---

## What this session did

Executed `memory/plan-company-case.md` in full, W1 through W6, then swept `docs/`.

Five commits, all pushed:

| Commit | What |
|---|---|
| `6cd5009` | `src/financials.py` and the case frame in the export |
| `52aa12c` | W1+W3+W4+W2+W5: exhibits restored, case framed, `/company` and `/story` built, deck polished |
| `3d8c0e6` | W6: `CLAUDE.md` swept, nine gotchas added, three corrections named |
| `45830cc` | The three `docs/` corrections |
| `f1704fe` | Remaining-work list corrected: three entries were already done |

### The audit this closed, in the numbers it used

| | `docs/index.html` | App before | App now |
|---|---|---|---|
| Exhibits | 18 | 11, four partial | **20** |
| Narrative steps with action titles | 22 | 0 | **18 on `/story`, parsed** |
| Client mentions | `storyline.md` only | none | `/`, `/company`, deck cover |
| Routes | n/a | 5 | **7** |
| Tests | n/a | 164 | **195** |

### New this session

- **`src/financials.py`**, the client's own P&L, unit economics and capital scale. Gated rows
  only, no new sourcing. Surfaces the **inverted RASK/CASK spread (4.99 against 5.00)**, which
  was arguably the sharpest number in the project and was on no surface.
- **`web/lib/exhibits.tsx`**, the exhibit registry keyed by the same `data-chart` ids
  `docs/index.html` uses. This is the thing that makes parity countable.
- **`web/components/DriverTree.tsx`**, the value-driver tree used as navigation.
- **`/story`** and **`/company`** routes.
- **`tests/test_delivery.py`** and **`tests/test_financials.py`**.
- `tests/test_narrative.py` now globs `web/**/*.tsx`, which was exempt for the app's whole
  life, and gained a paired-margin rule and a pivot-count claim.

---

## Three real problems found by checking, not by building

Worth recording because the pattern repeated and is the highest-yield activity on this repo.

1. **A load-factor claim the data contradicts.** `docs/storyline.md` claimed load factors had
   "recovered past their pre-pandemic level across every major carrier". DGCA 2019 against
   2025: IndiGo **87.8 to 86.1**, SpiceJet **92.7 to 86.2**, both DOWN. Only Air India and Air
   India Express are up. Withdrawn on both surfaces, guarded by `must_not_appear`.
2. **The pivot count was wrong in eight places**, six on seven surfaces and seven in
   `CLAUDE.md`, against eight entries in the log. Two were OpenGraph descriptions that show in
   link previews. It had drifted twice without failing anything. Only `web/app/methodology`
   was right, because it computes `pivots.length` rather than stating it.
3. **Three "remaining work" entries were already done** and had been carried for two sessions:
   Abu Dhabi's entitlement, belly cargo, and a forwarding note the user has now declined.

---

## Decisions taken this session

- **`/story` restored**, reversing an earlier assistant recommendation to drop it. The
  argument that a deck and a dashboard covered it was wrong: the action titles *were* the
  argument.
- **No aircraft price anywhere.** `capital_scale()` expresses the commitment in capacity and
  in revenue at realised RASK. A test fails if a price appears.
- **The published 0.31% block-hour reconciliation stays the headline.** It is scheduled-only;
  the like-for-like all-services figure is 0.64 hours in 1.6 million. Both are computed and
  reported, and 0.31% stays published so no other surface has to move.
- **The forwarding note is declined**, by the user, on 2026-08-19. It has been deleted from
  Next steps with a note not to re-add it. **Do not offer to write it.**
- **The three JD coverage gaps are terminal and should stay gaps.** Coverage is 82% and
  engineering it upward would defeat the analyzer's purpose.

---

## In-flight state the repo does not record

**Nothing.** Working tree is clean, no uncommitted files, no scratch directories, no dev
servers left running. `preview_stop` was called on both servers used this session.

Two `.claude/launch.json` configs exist: `site` (Python http.server on :8000, serves `docs/`)
and `app` (`npm --prefix web run dev` on :3000), the latter added this session. Use
`preview_start` with those names, never a bare `python -m http.server`, per gotcha 32.

---

## What a new session should do

**Ask before building.** The artifact is finished. The failure mode from here is churn.

Sensible opening moves, in order:

1. `python -m pytest -q` (offline, ~35s) and read `CLAUDE.md`.
2. Ask the user what they want. If they want work, the only unblocked item that would move a
   headline is the **BTS T-100 loader** for an India to United States both-ends
   reconciliation: it raises the **5.6%** cross-checked share that `docs/methodology.md` names
   as the honest limit of the whole provenance claim. BTS T-100 is free and machine-readable.
3. Everything else in Next steps is paywall-blocked (wide-body lease rates, Sharjah's MoU) or
   a convenience (data.gov.in ATF history).

**Do not** re-run `memory/plan-company-case.md`, re-add the forwarding note, or try to raise
the 82% coverage figure.

---

## The commands that matter

```bash
python -m pytest -q                      # offline, ~35s
python scripts/refresh.py --no-fetch     # rebuild figures from cached parquet
python -m src.app_export                 # rewrite web/public/data/ only
npx --prefix web tsc --noEmit            # types only, fast
npm --prefix web run build               # static export, what Vercel runs
```

**After changing any module that feeds a chart or the app, run BOTH `scripts/refresh.py
--no-fetch` and `python -m src.app_export`**, or the committed exports go stale and tests pass
against old data. Gotchas 10, 11 and the drift guard in `tests/test_app_export.py`.

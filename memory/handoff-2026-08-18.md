# Handoff, 2026-08-18

**Read `CLAUDE.md` first.** This file records only what the repo does not, plus the state at
the moment the session ended.

## Status in one line

Six planned pieces of work delivered, **all pushed**, 105 tests green, CI green on freshly
pulled data, Pages live at `b427d72`. **Nothing blocking.**

### Push outcome, 2026-08-18

Seven commits pushed clean, `b9348ac..b427d72`, fast-forward with no rebase needed. The
non-fast-forward rejection that gotcha 15 warns about did **not** occur, because CI had not
run since the last local sync.

Two things worth noting from the CI run afterwards:

- The workflow's "Tests again, against the newly pulled data" step passed, so the two new
  modules survive a fresh source pull and not merely the cached parquet.
- **CI committed nothing.** A full refresh from freshly pulled sources reproduced the
  committed chart JSON exactly, so there is no divergence to reconcile and the usual parquet
  churn (gotcha 14) did not appear this time.

## What this session was

The user brought an external review (from Fable) proposing a full rebuild of the project as
Next.js on Vercel with seven routes, an NPV model, Puppeteer PDF export, and a new palette.
The session evaluated it, took the analytical half, rejected the delivery half with reasons,
and then found that doing the analysis changed the answer.

### Decisions the user made, at the planning stage

Four forks were put to the user before any code was written. All four were answered with the
recommended option:

| Fork | Chosen |
|---|---|
| Delivery stack | **Enhance the static site in place.** No Next.js rebuild |
| Client framing | **Name IndiGo, keep the evidence base computed.** Not the full trade-press framing |
| Financial model | **Breakeven / value-at-stake**, not a four-seam NPV |
| Scope | **All four workstreams** |

These are settled. Do not relitigate them without the user reopening it.

### Three errors in the external review, caught and not carried across

1. It stated "India-UAE capped at 66,504 seats/week each side... a hard ceiling". That is
   gotcha 22: one emirate, one side, wrong by about 4x.
2. It recommended mirroring an Excel exhibit "for the JD". Violates a hard rule.
3. It claimed fuel and FX were not wired as sensitivity drivers. They already were, in
   `scenario.fuel_fx_sensitivity()`.

## The thing that actually happened: the answer changed

The headline was **"reclaim the Gulf corridor first, long-haul second"**. It is now
**"compete with the Gulf hubs, do not fly more aircraft to them"**, Europe first.

This was not a stylistic rewrite. It fell out of three findings that did not exist before:

- **No room.** India-Dubai runs at **89.6%** of its reported bilateral entitlement, about
  0.72M seats a year of headroom against a 36.9M corridor.
- **Worst unit economics on the map.** Gulf sectors have the least yield headroom of any
  corridor (**-4.3%**) and do not cover their own cost at IndiGo's realised yield.
- **An order book sized for a longer network.** 119.3bn ASK, **+78%** on today's international
  capacity, **1.94x** the growth needed to hold share. Clears only at sector +26.8% or share 58.2%.

The reconciliation: of 36.9M Gulf passengers, ~28.9M are genuinely point-to-point (Kerala
diaspora, already well served by narrow-bodies) and ~8.1M are connecting onward, worth
**INR 27,486 to 53,906 crore**. That second group is won by flying *past* the Gulf.

**The hypothesis tree had already reached this and nobody propagated it.** Branch 4.3 said
incremental Gulf capacity "has to go long-haul, or to Gulf points with slack, or wait on a
renegotiation India has so far refused". It sat there and never reached the headline. Branch
5.4 now asks whether the aircraft can be *deployed*, not just whether the demand exists, and
that question is what inverted the answer.

## Open decisions

- **The push.** 6 commits, public repo, user approval required. Nothing else is blocking.
- **Whether the case question itself should change.** The site's question is still "where
  should Indian carriers deploy their next 100 long-haul aircraft, and can the India-Gulf
  corridor absorb them?" The answer to the second half is now firmly "no, and that is the
  finding". The question still works, but a future session might sharpen it. Not raised with
  the user.
- **Wide-body lease rates** are the largest named unresolved input. The damp-lease bridge
  option in `docs/recommendation.md` is deliberately presented with its economics unquantified
  because IBA/Cirium rates are paywalled. A citable rate would make it comparable.

## In-flight state not in git

**Nothing.** Specifically checked at session end:

- Working tree clean, no untracked files outside the gitignored `data/raw/` cache
- **No preview servers running.** `preview_list` returns empty. The `site` server started
  during the session was stopped with `preview_stop`
- `.env` still does not exist (only needed for the optional data.gov.in ATF history)
- A full `python scripts/refresh.py --no-fetch` after the last commit produced **no diff**, so
  the committed chart JSON matches a fresh build

Two browser tabs remain open in the session preview (`file://.../docs/index.html` and
`http://localhost:8000`). The second points at a stopped server and is harmless.

## What was built, briefly

| Commit | What |
|---|---|
| `8d7733c` | Client brief + pivot log (five entries, later six) + inline callouts |
| `3ac7729` | `src/fleet_gap.py`. Order book in ASK. Also fixed the house-rule fixture gap |
| `fe43dd5` | `src/options.py`. Corridor breakeven, value at stake. Gated the O-D share |
| `9b796e7` | The recommendation change, `docs/recommendation.md`, risk register |
| `235dd56` | `docs/report.html`, print CSS, scenario selector, deleted `PROJECT_STATE.md` |
| `8da17a3` | Documentation reconciliation after the answer changed |

## Two process findings worth keeping

**The test suite was exempting four charts.** The house-rule fixture spanned only
`benchmarking` and `profit_pools`. `market_sizing` and `scenario` publish four charts between
them and had been outside the one-red and takeaway-title rules for the life of the project.
All four turned out compliant, but nothing would ever have caught it. There is now a test that
walks `src/` and fails if a publishing module is left out of `PUBLISHING_MODULES`.

**Changing a recommendation is a sweep, not an edit.** The answer changed in one commit and
left `storyline.md` contradicting itself (the Recommendation section rewritten, the Answer
section below it still arguing the old case in full), the hypothesis tree root stale, one
sentence in `index.html` referring to a headline that no longer existed, and
`data_dictionary.md` claiming "6 of 18 rows cleared" against an actual 25 of 30. All fixed in
`8da17a3`, and two drift-guard tests added. The working agreement in `CLAUDE.md` now names the
files that must be swept together.

## Where to pick up

1. Ask the user about the push. Expect a non-fast-forward rejection (gotcha 15); rebase and
   **verify actual values afterwards**, not just that tests pass.
2. Then re-verify the live site once Pages rebuilds, including `report.html`.
3. After that, `CLAUDE.md` "Next steps" 2 through 6 are all optional and none is blocking.


---

# Second half of 2026-08-18

Everything above still holds. What follows happened after that handoff was written.

## Status

**147 tests. Tree clean.** Live at GitHub Pages. Two things outstanding, both named below.

## What was built

| Commit | What |
|---|---|
| `67f09a5` | Abu Dhabi bilateral found (70.1% used, not capped like Dubai). Corrected an overclaim |
| `83cd131` | BTS T-100 retried and recorded as a **dead end** with specifics |
| `d01061c` | Headline figures moved **2024 to 2025**. 72.2M to 78.0M, band 91-108 to 96-109 |
| `79a7b2a` | Social card, favicon, chart accessibility (ARIA + data tables), `deck.html` |
| `a5995b9` | `tests/test_narrative.py` drift guard, and `src/cargo.py` |
| `812c375` | Two more stale figures found **on the live site** after the guard passed |
| `2d19f4d` | `docs/survey_design.md`, a conjoint instrument. Coverage deliberately stays 82% |
| `96447b9` | `docs/brief.html`, `scripts/make_pdfs.py`, committed PDFs |

## The two open items

**1. Vercel deploy. Needs the user, cannot be automated.**
`vercel.json` is committed. The user goes to vercel.com, signs in with GitHub, imports
`DogInfantry/india-widebody-window`, accepts the detected settings and deploys. **A
`.vercel.app` URL is what they want. They do NOT want a custom domain and were irritated by
repeated suggestions that a domain was the only reason to bother. Do not raise it again.**
Once the URL exists, repoint `og:url` in `index.html`, `report.html` and `brief.html`, the URL
baked into `scripts/make_social_card.py` (regenerate the PNG), and the README.

**2. The brief PDF page break does not land.**
`docs/india-widebody-brief.pdf` holds both audiences but the first spills onto the second
sheet. Content is all present; only the split is wrong. The full report PDF is fine at 30
pages. Already tried and did NOT work: `--headless=new`, `--no-pdf-header-footer` (this one
DID remove the Chrome header), and `break-after: page` beside `page-break-after: always`.
**The render came back byte-identical after the CSS change**, so check whether the
`@media print` rules for `.brief-page` reach the print context at all before trying more
break syntax.

## Two findings worth carrying forward

**Prose drifts from code and sweeps do not catch it.** Six stale figures surfaced in one day
across four methods: an audit script, the new guard, reading every numeric token on the page,
and fetching the deployed site. Each found what the previous missed. `tests/test_narrative.py`
now guards fifteen claims; its `must_not_appear` half is the one that works, and it only
catches wordings someone wrote down. **When a number moves, grep for how the PROSE words it,
not how the module prints it.**

**The cargo module nearly published the flattering answer.** The expected finding was that
freight reinforces long-haul. Stage length and freight per passenger correlate at **-0.10**.
North America is the longest corridor and carries less per passenger than Africa. The module
states that before the numbers. What survives: Europe carries 3.7x the Gulf's freight per
passenger, and East Asia is a 129.8 kg/pax outlier on a 2.0M-passenger corridor that nothing
in the case examines.

## Where the user actually is

**Applied to the BCN AMS Associate role. Silent.** They have a warm contact who could refer
them. The constraint is no longer the artifact, it is distribution: to use that contact the
person must forward a 17-chart site to someone senior. `docs/brief.html` and the PDFs exist to
remove that friction. **Still unwritten: a short note in the user's voice to send their
contact.** That was agreed and not delivered.


---

# Third block of 2026-08-18

Everything above still holds as history. **Both items it left open are now closed.**

## Status

**148 tests. 19 charts. Tree clean. Pushed at `434f186`, fast-forward, no rebase needed.**
Live on two hosts. Nothing in flight, no running servers, no untracked files.

## What changed

| What | Outcome |
|---|---|
| Vercel deploy | **Live and canonical:** https://india-widebody-window.vercel.app |
| Brief PDF page break | **Fixed.** One audience per sheet, 2 pages. Report still 30 |
| The Fable review | `docs/external_review_response.md`, item by item |

### Vercel: the account step was never needed

`vercel whoami` already returned `ankleshrawat5-8903` and CLI 54.10.3 was already installed at
`AppData/Roaming/npm/vercel`. Two prior handoffs recorded "the user must do the account step,
cannot be automated". **Check the tool before recording a blocker.** `vercel link --yes
--project india-widebody-window` then `vercel deploy --prod --yes` did the whole thing, and
`vercel git connect` reported the repo already connected, so `main` redeploys itself and
branches get preview URLs.

The first deploy **failed**: `No Flask entrypoint found`. `requirements.txt` at the repo root
makes Vercel detect a Python app. Fixed with `"framework": null` in `vercel.json`, plus
`.vercelignore` so nothing outside `docs/` is uploaded. Both committed.

GitHub Pages stays a working mirror and is still linked from `CLAUDE.md` and the README.

### The page break was a class collision, not break syntax

`docs/brief.html` carried `<body class="brief">`. `.brief` was already the decision-list grid
from `index.html` (`style.css:133`). So the brief's body was a **grid container** and its two
`.brief-page` sections were grid items, and Chrome does not honour a forced break between grid
items. `page-break-after: always` and `break-after: page` were both present, both correct and
both inert, which is exactly why the previous session's CSS change produced a byte-identical
PDF and looked like a print-context problem.

Body is `.brief-doc` now, four selectors renamed in the brief block of `style.css`. The page
was **also broken on screen** in three narrow columns, unnoticed because it had only ever been
rendered to PDF, never opened in a browser. A guard in `tests/test_narrative.py` pins the class
and was proven to fail when reverted.

## Open decisions

**None blocking.** The one thing that matters is not code: **the forwarding note to the warm
contact is still unwritten**, and it is now `CLAUDE.md` next step 1. Everything the note would
point at exists and is live.

## Verified at session end

- `git status` clean, `origin/main` at `434f186`, nothing ahead or behind
- `python -m pytest -q` 148 passed
- `refresh.py --no-fetch` produced no chart diff, so committed JSON matches a fresh build
- Live: `/`, `/report`, `/brief`, CSS, social card, both PDFs, chart JSON all 200 on Vercel;
  Plotly renders from that origin (3 SVGs, 2 traces, no console errors); Pages 200
- Brief PDF: 2 pages, page 1 the analysis only, page 2 the recruiter page only, both full
  width; report 30 pages; only URL printed anywhere in it is `india-widebody-window.vercel.app`
- No preview servers (`preview_stop` run), browser pane closed


---

# Fourth block of 2026-08-18: the delivery layer, and the audit that followed

## What happened

The user's Vercel item was closed by shipping the same static page on a new domain. That was a
hosting change, not a deliverable, and they said so. A full Next.js delivery layer was then
built in `web/` across six phases, all live and pushed at `8593d8a`.

Then they looked at it properly and the verdict was harder: barebones answer, no client, no
company work, no financials, "looks like a sector analysis", frameworks too thin, and the
question of why the new site is **worse and more scattered than the original GitHub Pages
page**.

They were right. The audit is the important artifact of this session.

## The audit

| | `docs/index.html` | The Next.js app | Lost |
|---|---|---|---|
| Exhibits | **18** | 11, four partial | **8** |
| Narrative steps with action titles | **22** | 0 | **22** |
| Client named on any surface | in `storyline.md` | **never** | the frame |

Dropped: `gateway_flows` (Sankey), `profit_pool` (Mekko), `fleet_gap`, `scenarios`,
`value_at_stake`, `who_carries_india`, `load_factor_slope`, `domestic_share`. Three of those
are the most distinctive visual forms in the project.

**The app was thinner than what it replaced, spread over five routes.** That is the whole
explanation for "scattered". Not arrangement: volume.

**And I recommended dropping `/story`, which was wrong.** The 22-step narrative was the
original's spine.

## What was already in the repo and never surfaced

`docs/storyline.md` holds the client (IndiGo, InterGlobe Aviation), the decision (60 A350-900s
firm plus 40 purchase rights), the timeframe, full SCQA, and success metrics including
**RASK 4.99 against CASK 5.00, FY2026, currently inverted**. The analysis was always
IndiGo-anchored. Only the delivery was not.

`data/manual/assumptions.csv` already carries revenue FY25 80,803 and FY26 84,962 crore,
EBITDAR 21,252 and 23,189, reported margin 17.8% and ex-forex 27.3%, CASK/RASK/yield, ATF,
utilisation, and **Gulf carrier yield 9.924 against IndiGo's 5.06**, which is the competitive
pricing evidence. All gated. A financial layer needs no new sourcing.

## Decisions taken with the user

- Financials: **P&L, unit economics and capital scale only.** No balance sheet, no ROIC, no
  WACC, and the page says so.
- Structure: **add `/company`**, keep every existing route.
- Driver tree: revenue, profit, reach and competitive position, as **one identity decomposed**
  rather than four frameworks stacked, and it doubles as the site navigation.
- Deck: all four kinds of polish, including presenter notes and a five versus fifteen minute
  path.
- **Nothing is erased.** `docs/` stays byte-unchanged.

## The plan

`memory/plan-company-case.md`, approved and NOT started. Six workstreams, ordered W1, W3, W4,
W2, W5, W6. It also carries a design contract that is not optional: one tabbed exhibit grammar,
chart form chosen by the question, one annotated point per exhibit, and a written list of what
not to add.

## State at handoff

164 tests. Tree clean. `origin/main` at `8593d8a`, pushed, git-connected Vercel build verified
Ready in 33s. Five routes live plus the Pages mirror. `requirements.txt` still seven packages.
Seven pivots in the log. Assumptions 31, 25 cleared, six terminal.

Non-code item still open across four sessions: **the forwarding note to the warm contact.**

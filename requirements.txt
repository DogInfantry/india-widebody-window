# Kept deliberately small. Every entry earns its place.
# Rejected during planning: duckdb (90k rows, pandas suffices), pyopensky
# (superseded by DGCA city-pair data), pdfplumber (no PDF source retained),
# vizro/dash (needs a Flask process, incompatible with static GitHub Pages).

pandas>=2.2
numpy>=1.26
pyarrow>=15.0
requests>=2.31
lxml>=5.0
plotly>=6.0
pytest>=8.0
